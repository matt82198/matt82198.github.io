{
  "name": "matt82198.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "python scripts/sync-aesop-stats.py && node scripts/fetch-medium.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "sync:stats": "python scripts/sync-aesop-stats.py"
  },
  "dependencies": {
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.61.1"
  }
}
