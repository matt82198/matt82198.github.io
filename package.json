{
  "name": "matt82198.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "python scripts/sync-aesop-stats.py && node scripts/fetch-medium.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "sync:stats": "python scripts/sync-aesop-stats.py",
    "lint:sources": "node scripts/check-numeric-sources.mjs --strict",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "playwright": "^1.61.1"
  }
}
