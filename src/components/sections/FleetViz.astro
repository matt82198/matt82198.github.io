---
// FleetViz section — data visualizations of agent fleet behavior
---

<section class="fleet" id="fleet">
  <p class="section-moral"><em>The shape of work reveals the shape of thought.</em></p>
  <h2>Fleet Visualizations</h2>
  <p>Real-time dashboards and post-hoc analysis of distributed agent behavior. Cost accounting, latency profiles, orchestration topologies.</p>
</section>

<style>
  .fleet {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
