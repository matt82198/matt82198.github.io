---
// ClientWork section — client projects
---

<section class="clientwork" id="clientwork">
  <p class="section-moral"><em>The finest work is invisible: it solves problems the client forgot they had.</em></p>
  <h2>Client Work</h2>
  <p>Full-stack product engineering for startups. Architecture, backend, DevOps, and systems design.</p>
</section>

<style>
  .clientwork {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
