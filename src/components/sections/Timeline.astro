---
// Timeline section — professional history and milestones
---

<section class="timeline" id="timeline">
  <p class="section-moral"><em>The arc of work is long, but readable in hindsight.</em></p>
  <h2>Timeline</h2>
  <p>14 years building distributed systems, data infrastructure, and developer tools. From query engines to orchestration harnesses.</p>
</section>

<style>
  .timeline {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
