---
// Aesop section — orchestration harness work
---

<section class="aesop" id="aesop">
  <p class="section-moral"><em>The orchestra sounds best when every musician listens to the whole.</em></p>
  <h2>Work: Aesop Harness</h2>
  <p>Open-source fable-fleet orchestration for Claude Code — autonomous Haiku agents, parallel waves, durable state, secret-scan gates.</p>
</section>

<style>
  .aesop {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
