---
// Writing section — essays and documentation
---

<section class="writing" id="writing">
  <p class="section-moral"><em>Write what you had to learn; leave the map for the next traveler.</em></p>
  <h2>Writing</h2>
  <p>Essays on distributed systems, orchestration patterns, and AI agent design. Doctrine papers on state machines and reliable handoff.</p>
</section>

<style>
  .writing {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
