---
// TwoAIReview section — multi-LLM evaluation and review systems
---

<section class="review" id="review">
  <p class="section-moral"><em>Two eyes see what one cannot; wisdom is knowing which to trust.</em></p>
  <h2>Two-AI Review</h2>
  <p>Comparative analysis frameworks for LLM systems. Cross-model verification, adversarial prompt design, and multi-stage critique.</p>
</section>

<style>
  .review {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
