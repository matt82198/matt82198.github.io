---
// PrivateWork section — internal / unreleased work
---

<section class="privatework" id="privatework">
  <p class="section-moral"><em>Some tools are sharper than they first appear.</em></p>
  <h2>Private Work</h2>
  <p>Unreleased: orchestration frameworks, state-machine compilers, and fleet audit suites built on the Aesop substrate.</p>
</section>

<style>
  .privatework {
    margin-bottom: var(--space-20);
  }

  .section-moral {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    margin-bottom: var(--space-4);
  }

  .section-moral em {
    font-style: italic;
    color: var(--color-text-tertiary);
  }

  h2 {
    color: var(--color-text);
    margin-bottom: var(--space-6);
  }

  p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
</style>
